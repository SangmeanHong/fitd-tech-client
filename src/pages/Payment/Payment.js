import React from 'react'
import Pricing from '../Home/Pricing'

export const Payment = ({ }) => {
    // const userId = userId;

    return (
        <div>
            {/* {
                !userId && */}
            <Pricing />
            {/* } */}
        </div>
    )
}
